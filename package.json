{
  "name": "adventjs",
  "version": "1.0.0",
  "description": "Project to get better at solving problems with TS",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "npx nodemon",
    "build": "rimraf ./build && tsc",
    "start": "npm run build && node build/index.js"
  },
  "keywords": [],
  "author": "Zabdiel Rosas",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.7.0",
    "nodemon": "^3.0.1",
    "rimraf": "^5.0.5",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  }
}
